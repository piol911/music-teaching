# 音乐教学演示台 · 项目文档

> 本文件是该项目的「单一事实来源（Single Source of Truth）」，用于跨会话续接。
> 任何新会话开始时，先读取本文件即可恢复全部上下文。
> 配套记忆：`/Users/lxy/.codebuddy/projects/Users-lxy/memory/project_music_teaching_app.md`

---

## 一、项目定位

| 项 | 内容 |
|---|---|
| 名称 | 音乐教学演示台（Music Teaching Studio） |
| 使用者 | 中职音乐教师（用户本人） |
| 用途 | 课堂演示：登录后全屏播放乐谱图片 / 视频 / 音频 / 文字，教师可随时编辑内容 |
| 形态 | 纯前端静态网页（无构建步骤），数据用 Supabase 做多设备云同步 |
| 当前本地版本 | **v16**（目录 `music-teaching-app/`） |
| 线上版本 | **v16**（已部署）→ https://aaaad46c733d412bb948d915fcbd2b28.app.codebuddy.work |

> 本地已迭代到 **v13**（单元内幻灯片拖拽排序）。

**为什么做这些事**：用户用它做课堂演示，需要稳定可分享的线上地址，且经常要加小功能、改内容。沟通中多次出现「按钮点了没反应」「翻页不灵」——最终定位多为浏览器缓存了旧 `app.js`。

---

## 二、技术架构与文件

```
music-teaching-app/
├── index.html        # 单页结构：登录 / 仪表盘 / 演示 / 管理 / 文件管理 / 弹窗 / lightbox / 视频弹窗
├── style.css         # 全部样式（极简、留白、蓝橙主色，含移动端适配）
├── app.js            # 全部逻辑（演示控制、Supabase 读写、管理、拖拽、全屏等）
├── data.js           # DEFAULT_DATA：人教版音乐鉴赏教材示例内容（11+ 单元）
└── media/textbook/   # 乐谱/教材图片（封面、歌曲配图等）
```

- 第三方依赖：`@supabase/supabase-js@2`（CDN 引入），无 npm 依赖、无打包。
- 数据表：`units`（id, name, description, sort_order）、`slides`（id, unit_id, title, image_url, video_url, audio_url, text_content, sort_order）、`app_settings`（password）、Storage 桶 `media`。
- 单元（unit）= 一个「课程单元 / 章节」；slide = 单元内的一页（图片/视频/音频/文字）。
- **`units.description` 列现复用为「章节图标」**：仅当内容短（≤4 字符，即 emoji）时视为图标，长文本（默认数据的描述）不当作图标，避免把句子显示成图标。

---

## 三、功能演进（关键节点）

| 版本 | 内容 |
|---|---|
| v1–v5 | 基础框架：登录、仪表盘单元卡片、演示播放、内容管理弹窗 |
| v6 | Lightbox 动画消除残影；视频融入图片队列；文字区 50/50 布局 |
| v7 | 底部固定缩略图横条；手机滚动修复；文字美化 |
| v8 | 首页放大去密码；管理内容密码门；多视频；文件管理页；点击空白翻页 |
| v9 | 演示模式「目录」浮层（右侧滑出当前单元幻灯片列表可跳转） |
| **v10** | 「全屏」按钮（F 快捷键；Esc 先退全屏再退演示） |
| **v11** | 演示屏重构：顶部只留标题，底部极简图标栏（退出=箭头 / 目录=三横杠 / 管理=三角形 / 全屏=方框），无文字；修复「点空白左右半屏翻页」 |
| **v12** | 图标按钮尺寸对齐翻页键(52px)；管理页「单元弹窗」新增「封面文字」字段（写入封面页 slide）；仪表盘单元卡片拖拽排序（写回 `units.sort_order`） |
| **v13** | 管理页单元内幻灯片拖拽排序：每个单元下的页面可拖拽重排，写回 `slides.sort_order`；封面页锁定 |
| **v14** | 改用 ↑/↓ 按钮排序（替换 HTML5 拖拽，因触屏拖拽无效）；单元卡片 + 幻灯片均加 ↑/↓ |
| **v15** | 浏览界面去掉排序按钮；页面顺序仅在管理页用 ↑/↓；演示底部按钮全面放大（桌面 64 / 手机 58 / 极窄 52px，修复手机媒体查询压到 46/38 的坑） |
| **v16** | 仪表盘单元卡片**精简**：去掉「X 个页面 / X 种类型」与类型标签，只留图标 + 名称。**新增章节图标编辑**：新建/修改单元弹窗加图标选择器（16 个音乐/课堂 emoji），存入 `units.description` 列（短字符串视为图标，长文本不认，兼容旧数据）；卡片优先用自定义图标否则按内容派生。调整图标框尺寸（cover 138px / 图标 3.4rem）、卡片 hover 轻微缩放、管理页单元区块淡入动画 |

### 演示模式底部图标栏（v11 定稿）
- 左：`退出`（← 箭头） · 中：`上一页 / 进度圆点 / 下一页` · 右：`目录`（☰ 三横杠）/ `管理`（△ 三角形）/ `全屏`（方框）
- 底部栏常驻可见（不再自动隐藏），避免「按钮点了没反应」。
- 翻页：点舞台**左半屏=上一页、右半屏=下一页**（图片/视频/音频/按钮/缩略图/控制栏等真正可交互元素除外，文字与留白均翻页）。
- 快捷键：←/→ 或空格翻页，F 全屏，Esc 退全屏→再按退演示。

---

## 四、本地运行与部署

### 本地预览
```bash
cd /Users/lxy/WorkBuddy/2026-07-21-22-04-16/music-teaching-app/
python3 -m http.server 8080
# 浏览器打开 http://localhost:8080 ，改完硬刷新 Cmd+Shift+R
```

### 部署（线上分享地址）
- 通过本机运行的 **WorkBuddy MCP** 代理（`http://127.0.0.1:60581/mcp`）的 `workbuddy_cloudstudio_deploy` 工具部署，**需 WorkBuddy.app 在运行**。
- ⚠️ 本 CodeBuddy Code CLI 会话**未挂载该 MCP**，无法直接部署；需用户在本地确认 WorkBuddy.app 运行后，由具备该 MCP 的环境执行。
- ⚠️ 坑：该工具会复用同目录旧沙箱，旧文件存在导致上传 409 → 先删除 `~/.workbuddy/cloudstudio-deploy-history/` 下该目录（`deployTargetId` 形如 `31ebcd96dfeb2a25_*`）的历史 JSON，再部署即建干净新沙箱。
- 沙箱有时效，过期后需重部署（网址会变）。
- 当前线上地址（v16，已部署）：`https://aaaad46c733d412bb948d915fcbd2b28.app.codebuddy.work`

---

## 五、用户画像与协作偏好（来自记忆）

- **语言**：始终用中文回复。
- **思考可见**：先展示推理过程，再给结论（用户明确要「看思考」）。
- **身份**：中职音乐教师，做课堂演示；非程序员，描述问题偏「现象级」（如「点了没反应」），需要我主动定位根因而非照字面做。
- **审美**：偏好极简设计语言（无文字、纯图标、箭头/三横杠/三角形等）。
- **其他**：在家玩咖啡（手冲+半自动），常招待朋友（与项目无关，建立关系的闲聊素材）。

---

## 六、待办 / 下一步（可选）

- [x] 重新部署到线上（v16）：https://aaaad46c733d412bb948d915fcbd2b28.app.codebuddy.work
- [x] 排序：仅在**管理页**用 ↑/↓ 调整页面顺序（v15）；浏览界面不加排序按钮；原 HTML5 拖拽因触屏无效已弃用。
- [ ] 演示模式「全屏」按钮已做（v10）。
- [ ] 如用户再报「按钮无反应 / 翻页不灵」，第一怀疑：**浏览器缓存旧 app.js** → 硬刷新或部署新地址。

---

## 七、沟通纪要（关键结论备查）

1. **「按钮点了没反应 / 空白翻页失效」**：99% 是浏览器缓存了旧 `app.js`（HTML 已更新但 JS 还是旧版，导致新按钮 `btn-admin-pres` 无 handler、翻页沿用旧排除名单）。解法：硬刷新（Cmd+Shift+R）或部署新地址。代码本身经核查无误（所有函数与绑定均在、辅助函数均定义、`.toc-overlay` 隐藏态 `pointer-events:none` 不会挡点击、底部栏 `z-index:20` 在舞台之上）。
2. **图标按钮太小**：已统一为 52px，与翻页键一致。
3. **封面文字**：在「新建/修改单元」弹窗加「封面文字」字段，映射到该单元封面页（第一张 slide）的标题+正文并持久化。
4. **章节排序**：仪表盘单元卡片 HTML5 拖拽，落点重排后写回 `units.sort_order`。

---

*最后更新：2026-07-27（v12）*
